const discover = '/discover';

const daily_recommend = `${discover}/daily_recommend`;

const album = '/album';
const song_list = '/album';

const like = '/like';

const search = '/search';

export default {
    discover,
    daily_recommend,
    alb